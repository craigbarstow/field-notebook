<!DOCTYPE html>
<html>
<head>
  <title>FieldNotebook</title>
  <%= stylesheet_link_tag 'application', media: 'all' %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div id="background-img"></div>

  <div class="container">
  <div class="row">
    <div class="column">
      <div id="devise-wrapper">
        <%- flash.each do |key, value| -%>
          <div class="flash flash-<%= key %>">
            <%= value %>
          </div>
        <%- end -%>
        <%= yield %>
      </div>
    </div>
  </div>
</div>

  <%= javascript_include_tag 'application' %>
</body>
</html>
