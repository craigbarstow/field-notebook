<div class="small-12 columns img-wrapper" id="edit-img-wrapper">
  <%= link_to "< Back", project_path(@project), class: 'button radius back-btn' %>
  <h1>Edit Photo</h1>
  <%= form_for [@project, @photo] do |f| %>
    <%= f.label :title %>
    <%= f.text_field :title %>

    <%= f.label :caption %>
    <%= f.text_area :caption %>

    <%= f.submit "Update Information", class: "button radius" %>
  <% end %>
<div>
